<div class="grid_3">
<% title = admin_signed_in? ? 'Add' : 'Suggest' %>
<h1><%= title %> New Place</h1>

<%= form_for @place, :url => { :action => :create} do |p| %>
<%= render :partial => 'form',  :locals => { :p => p } %>
<% end %>

<%= link_to 'Back', places_path%>
</div>

<div class="grid_6">
    <div id="map_canvas" style="width: 500px; height: 500px; margin: auto;"></div>
</div>
