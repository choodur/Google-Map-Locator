 <% if @place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@place.errors.count, "error") %> prohibited this author from being added:</h2>

      <ol>
      <% @place.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ol>
    </div>
  <% end %>
  <%= flash[:error] if flash[:error] %>
  <div class="field">
    <%= p.label :name %><br />
    <%= p.text_field :name%>
  </div>
  
  <div class="actions">
    <%= p.submit %>
  </div>
